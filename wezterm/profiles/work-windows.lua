-- Keep the work profile conservative until the machine's WSL and policy
-- restrictions are known. Add approved settings here once the machine arrives.
return {
  apply_to_config = function() end,
}
